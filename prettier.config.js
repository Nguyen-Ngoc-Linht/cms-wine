// https://prettier.io/docs/en/options.html#quote-props
module.exports = {
  printWidth: 100, // Length per line of code (default80)
  tabWidth: 2, // eachtabequivalent to how many spaces (default2)
  useTabs: false, // use or nottabindent (defaultfalse)
  singleQuote: true, // use single quotes
  semi: false, // Do not use a semicolon at the end of the declaration(defaulttrue)
  trailingComma: 'es5', // Use trailing commas for multiple lines (defaultnone)
  bracketSpacing: true, // Use spaces between curly braces in object literals (defaulttrue)
  arrowParens: 'avoid', // Arrow function parameters can be omitted when parentheses can be omitted
  jsxBracketSameLine: true, // Set astrueTime,multiple linesJSXelemental > at the end of the last line, not on the next line by itself
  bracketSameLine: false, //
  singleAttributePerLine: true,
  // indentSize: 8,
}
